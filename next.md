# GigHub Blueprint 架構說明

## 🏗️ GigHub 工地施工進度追蹤管理系統 - Blueprint 架構

本專案採用 **Blueprint Container 架構**，將系統分為**兩層**：

### 🔷 架構概覽

```
GigHub Blueprint 系統
├── 🟦 (A) Platform Layer（平台層）—— 非業務域
└── 🟥 (B) Business Domains（業務域）—— 6～8 個模塊
```

---

## 🟦 (A) Platform Layer（平台層）—— 非業務域

這層是**讓所有業務域模塊能運作的基礎系統層**，不屬於任何特定業務。

### 平台層組成

1. **Context Module（上下文模組）**
   - Blueprint 配置管理
   - 執行時上下文
   - 狀態機配置
   - 業務域啟用設定
   - 共用記憶體（Shared Memory）
   - 跨模組資料代理

2. **Event / Automation Engine（事件/自動化引擎）**
   - 業務域間事件交換
   - Workflow 驅動
   - 自動化觸發器

3. **Container（容器）**
   - 模塊生命週期管理
   - 依賴注入
   - 模塊載入與卸載

### 平台層比喻

```
Task Domain     = 應用程式 (App)
Finance Domain  = 應用程式 (App)
QA Domain       = 應用程式 (App)
Material Domain = 應用程式 (App)
───────────────────────────────
Context Module  = 作業系統 (OS)  ← 所有 App 都跑在它上面
```

---

## 🟥 (B) Business Domains（業務域）—— 我們的 Blueprint 模塊

這些是**真正的業務邏輯模塊**，每個模塊負責特定的業務範圍。

### ✅ 必要業務域（6 個）

1. **Task Domain（任務域）** ✅ 已實作
2. **Log Domain（日誌域）** 🔴 待實作
3. **Workflow Domain（流程域）** 🔴 待實作
4. **QA Domain（品質控管域）** 🔴 待實作
5. **Acceptance Domain（驗收域）** 🔴 待實作
6. **Finance Domain（財務域）** 🔴 待實作

### 🟡 推薦業務域（1～2 個）

7. **Material Domain（材料域）** 🔴 待實作
8. **Safety / Communication Domain（安全/通訊域）** 🔴 可選

---

## 📌 關鍵原則

- **Context Module 不是業務域**：它是平台層，屬於基礎設施
- **Blueprint 的業務域數量**：6～8 個（不包含 Context）
- **模塊間關係**：通過 Event Bus 解耦，避免直接依賴
---

## 📋 各業務域詳細說明

### 1️⃣ Task Domain（任務域）✅ 已實作

**核心功能**：所有行為的中心

**包含功能**：
- 任務（Task）建立、編輯、刪除
- 任務指派（Assignment）
- 狀態機（State Machine）管理
- 進度（Progress）追蹤
- 排程（Schedule）管理
- 子任務（Subtask）支援

**模塊 ID**: `tasks`

---

### 2️⃣ Log / Activity Domain（日誌域）🔴 待實作

**核心功能**：追蹤所有系統變更與使用者行為

**包含功能**：
- 操作紀錄（Activity Log）
- 系統事件（Event）
- 評論（Comment）
- 附件（Attachment）
- 變更歷史（Change History）

**模塊 ID**: `log` 或 `activity`

**依賴關係**：非常多模塊會依賴此模塊（Task、QA、Finance 等）

---

### 3️⃣ Workflow / Process Domain（流程域）🔴 待實作

**核心功能**：所有可組態的流程管理

**包含功能**：
- 自訂流程（Custom Workflow）
- 自定義狀態圖（State Machine）
- 自動化事件（Automation Trigger）
- 流程模板（Workflow Template）
- 審批流程（Approval Process）

**模塊 ID**: `workflow`

**依賴關係**：Task、Finance、QA、Acceptance 都會用到

---

### 4️⃣ QA Domain（品質控管域）🔴 待實作

**核心功能**：施工品質管理與檢查

**包含功能**：
- 例行檢查（Checklist）
- 缺失紀錄（Issue/Defect）
- 缺失修復流程
- 現場巡檢（Inspection Log）
- 品質報告（QA Report）

**模塊 ID**: `qa` 或 `quality`

---

### 5️⃣ Acceptance Domain（驗收域）🔴 待實作

**核心功能**：正式驗收流程（與 QA 分開，流程不同）

**包含功能**：
- 驗收申請（Acceptance Request）
- 驗收審核（Acceptance Review）
- 初驗（Preliminary Acceptance）
- 複驗（Re-inspection）
- 驗收結論（Pass/Fail）

**模塊 ID**: `acceptance`

---

### 6️⃣ Finance Domain（財務域）🔴 待實作

**核心功能**：財務管理（這是一個域，底下可多個子功能）

**包含功能**：
- 成本管理（Cost Management）
- 請款（Invoice）
- 付款（Payment）
- 預算（Budget）
- 帳務（Ledger）
- 財務報表（Financial Report）

**模塊 ID**: `finance`

**重要**：財務域是「一個模塊」，不是多個模塊

---

### 7️⃣ Material / Asset Domain（材料/資產域）🟡 推薦實作

**核心功能**：材料與資產管理

**包含功能**：
- 材料管理（Material Management）
- 材料領用（Material Issue）
- 出入庫（Inventory In/Out）
- 器具資產（Equipment/Asset）
- 損耗記錄（Consumption Record）

**模塊 ID**: `material`

---

### 🔥 可選業務域（成熟後再加）

#### 8️⃣ Safety Domain（安全域）

**包含功能**：
- 安全巡檢
- 風險評估
- 事故通報
- 安全教育訓練

**模塊 ID**: `safety`

#### 9️⃣ Communication / Message Domain（訊息域）

**包含功能**：
- 系統通知
- 群組訊息
- 任務提醒
- 推播通知

**模塊 ID**: `communication` 或 `message`

---

### 0️⃣ Shared Infrastructure（共享基礎設施）

**注意**：這不算獨立業務域，屬於基礎設施（infra）

**包含**：
- 使用者管理（User）
- 權限管理（Permission）
- 組織/僱主（Organization/Employer）
- 團隊/機器人（Team/Bot）
- Blueprint 元資料（Metadata）

**位置**：`src/app/core/` 或 `src/app/shared/`

---

## 🗂️ GigHub 專案實際目錄結構

### 完整結構樹

```
GigHub/
├── src/app/
│   ├── core/
│   │   ├── blueprint/                      # Blueprint 核心系統
│   │   │   ├── config/                     # Blueprint 全域配置
│   │   │   ├── container/                  # 模塊容器（生命週期管理）
│   │   │   ├── context/                    # 執行上下文（平台層）
│   │   │   │   ├── execution-context.interface.ts
│   │   │   │   └── execution-context.service.ts
│   │   │   ├── events/                     # 事件系統（平台層）
│   │   │   │   ├── event-bus.service.ts
│   │   │   │   └── event.interface.ts
│   │   │   ├── integration/                # 外部整合服務
│   │   │   ├── models/                     # Blueprint 模型定義
│   │   │   │   └── blueprint-module.model.ts
│   │   │   ├── modules/                    # 模塊系統
│   │   │   │   ├── module.interface.ts    # IBlueprintModule 介面
│   │   │   │   ├── module-status.enum.ts  # 模塊狀態列舉
│   │   │   │   └── implementations/        # 扁平化模塊實作目錄
│   │   │   │       ├── tasks/              # ✅ 任務域模塊（已實作）
│   │   │   │       │   ├── tasks.module.ts
│   │   │   │       │   ├── tasks.service.ts
│   │   │   │       │   ├── tasks.repository.ts
│   │   │   │       │   ├── tasks.component.ts
│   │   │   │       │   ├── tasks.routes.ts
│   │   │   │       │   ├── module.metadata.ts
│   │   │   │       │   └── views/          # 任務相關視圖
│   │   │   │       ├── climate/            # ✅ 氣候模塊（已實作，非必要業務域）
│   │   │   │       │   ├── climate.module.ts
│   │   │   │       │   ├── services/
│   │   │   │       │   ├── repositories/
│   │   │   │       │   └── config/
│   │   │   │       ├── log/                # 🔴 日誌域模塊（待實作）
│   │   │   │       ├── workflow/           # 🔴 流程域模塊（待實作）
│   │   │   │       ├── qa/                 # 🔴 品質控管域模塊（待實作）
│   │   │   │       ├── acceptance/         # 🔴 驗收域模塊（待實作）
│   │   │   │       ├── finance/            # 🔴 財務域模塊（待實作）
│   │   │   │       ├── material/           # 🔴 材料域模塊（待實作，推薦）
│   │   │   │       ├── safety/             # 🔴 安全域模塊（待實作，可選）
│   │   │   │       └── communication/      # 🔴 通訊域模塊（待實作，可選）
│   │   │   ├── repositories/               # Blueprint 資料存取層
│   │   │   └── services/                   # Blueprint 共享服務
│   │   ├── services/                       # 核心服務（非 Blueprint）
│   │   ├── models/                         # 核心模型
│   │   └── ...
│   ├── routes/                             # 路由模塊（UI 層）
│   ├── layout/                             # 版面配置
│   ├── shared/                             # 共享元件
│   └── features/                           # 功能模塊（可選）
└── ...
```

### 關鍵說明

1. **扁平化模塊結構**
   - 所有業務域模塊都放在 `modules/implementations/` 下
   - 每個模塊自成一個資料夾，自包含所有相關檔案
   - 不使用巢狀子資料夾分類

2. **平台層位置**
   - `context/` - 執行上下文服務
   - `events/` - 事件匯流排
   - `container/` - 模塊容器
   - 這些都在 `blueprint/` 下，與 `modules/` 同層

3. **模塊內部結構**（以 tasks 為例）
   ```
   tasks/
   ├── tasks.module.ts          # 實作 IBlueprintModule
   ├── tasks.service.ts         # 業務邏輯服務
   ├── tasks.repository.ts      # 資料存取（Supabase）
   ├── tasks.component.ts       # UI 元件
   ├── tasks.routes.ts          # 路由配置
   ├── module.metadata.ts       # 模塊元資料
   ├── models/                  # 模塊專用模型（可選）
   ├── views/                   # 子視圖元件（可選）
   └── index.ts                 # 匯出檔
   ```

---

## 🏷️ 模塊命名規範

### 命名原則：「域-模塊功能名稱」

為了保持 `modules/implementations/` 資料夾扁平化，我們採用以下命名規範：

### 1. Module ID（模塊識別碼）

**格式**：單一詞彙，全小寫，使用短橫線連接（如有需要）

**範例**：
```typescript
// ✅ 正確
id: 'tasks'          // 任務域
id: 'log'            // 日誌域
id: 'workflow'       // 流程域
id: 'qa'             // 品質控管域
id: 'acceptance'     // 驗收域
id: 'finance'        // 財務域
id: 'material'       // 材料域

// ❌ 錯誤（不要使用前綴）
id: 'domain-tasks'
id: 'blueprint-tasks'
```

### 2. 資料夾名稱

**格式**：與 Module ID 一致

**範例**：
```
modules/implementations/
├── tasks/           # 任務域
├── log/             # 日誌域
├── workflow/        # 流程域
├── qa/              # 品質控管域
├── acceptance/      # 驗收域
├── finance/         # 財務域
└── material/        # 材料域
```

### 3. 模塊檔名

**格式**：`{module-id}.module.ts`

**範例**：
```typescript
tasks/tasks.module.ts           // 任務域模塊主檔
log/log.module.ts               // 日誌域模塊主檔
workflow/workflow.module.ts     // 流程域模塊主檔
qa/qa.module.ts                 // 品質控管域模塊主檔
```

### 4. 服務、Repository、Component 命名

**格式**：`{module-id}.{type}.ts`

**範例**：
```
tasks/
├── tasks.module.ts
├── tasks.service.ts
├── tasks.repository.ts
├── tasks.component.ts
└── tasks.routes.ts
```

### 5. 模塊元資料檔案

**格式**：`module.metadata.ts`（固定名稱）

**範例**：
```typescript
// tasks/module.metadata.ts
export const TASKS_MODULE_METADATA = {
  id: 'tasks',
  moduleType: 'tasks',
  name: '任務管理',
  nameEn: 'Task Management',
  version: '1.0.0',
  // ...
};
```

### 6. 完整範例：Finance Domain（財務域）

```
modules/implementations/finance/
├── finance.module.ts              # 實作 IBlueprintModule
├── finance.service.ts             # 業務邏輯
├── finance.repository.ts          # 資料存取
├── finance.component.ts           # UI 元件
├── finance.routes.ts              # 路由
├── module.metadata.ts             # 模塊元資料
├── models/                        # 財務專用模型
│   ├── cost.model.ts
│   ├── invoice.model.ts
│   └── payment.model.ts
├── views/                         # 子功能視圖
│   ├── cost-management/
│   ├── invoice-management/
│   └── payment-management/
└── index.ts                       # 匯出
```

---

## 📊 模塊實作優先級

| 優先級 | 模塊名稱 | Module ID | 狀態 | 原因 |
|--------|----------|-----------|------|------|
| ✅ P0 | Task Domain | `tasks` | ✅ 已完成 | 系統核心 |
| 🔴 P1 | Log Domain | `log` | 🔴 待實作 | 必要：追蹤所有變更 |
| 🔴 P1 | Workflow Domain | `workflow` | 🔴 待實作 | 必要：多模塊依賴 |
| 🔴 P2 | QA Domain | `qa` | 🔴 待實作 | 必要：品質管理 |
| 🔴 P2 | Acceptance Domain | `acceptance` | 🔴 待實作 | 必要：正式驗收 |
| 🔴 P2 | Finance Domain | `finance` | 🔴 待實作 | 必要：財務管理 |
| 🟡 P3 | Material Domain | `material` | 🔴 待實作 | 推薦：材料管理 |
| 🟢 P4 | Safety Domain | `safety` | 🔴 待實作 | 可選：安全管理 |
| 🟢 P4 | Communication Domain | `communication` | 🔴 待實作 | 可選：訊息通知 |

---

## 🎯 總結

### GigHub Blueprint 架構特點

1. **兩層架構**
   - 平台層（Platform Layer）：Context、Events、Container
   - 業務域（Business Domains）：6～8 個業務模塊

2. **扁平化模塊組織**
   - 所有模塊都在 `modules/implementations/` 下並列
   - 不使用巢狀分類資料夾
   - 模塊內部自行組織檔案結構

3. **清晰的命名規範**
   - Module ID：單一詞彙（如 `tasks`, `finance`）
   - 資料夾名稱：與 Module ID 一致
   - 檔案命名：`{module-id}.{type}.ts`

4. **模塊職責分明**
   - 每個模塊負責單一業務域
   - 模塊間通過 Event Bus 通訊
   - 避免直接依賴

5. **可擴展性**
   - 新增模塊只需在 `implementations/` 下建立新資料夾
   - 實作 `IBlueprintModule` 介面
   - 註冊到 Container 即可

---

## 📚 相關文件

- `BLUEPRINT_ANALYSIS_README.md` - Blueprint 架構分析總覽
- `src/app/core/blueprint/modules/module.interface.ts` - IBlueprintModule 介面定義
- `src/app/core/blueprint/modules/implementations/tasks/` - 任務域模塊實作範例
- `src/app/core/blueprint/container/` - 模塊容器實作

